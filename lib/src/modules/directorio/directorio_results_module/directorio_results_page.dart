import 'package:flutter/material.dart';

class DirectorioResultsPage extends StatelessWidget {
  const DirectorioResultsPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Resultados'),
    ),
    body: const Center(
      child: Text('Results Page Placeholder'),
    ),
  );
}

import 'package:flutter/material.dart';

class DirectorioFiltrosPage extends StatelessWidget {
  const DirectorioFiltrosPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Filtros'),
    ),
    body: const Center(
      child: Text('Filtros Page Placeholder'),
    ),
  );
}
